arrays
# array
- definition
```$xslt
    int[] arr=new int [10];
```

- input
```$xslt
    for(int i=0;i<arr.length;i++)
        arr[i]=i*i;
```
- output
```$xslt
    for(int i=0;i<arr.length;i++)
        System.out.println(arr[i]);
```
# array app

## search
    - linearSearch
    - binarySearch
## sorting
    - SimpleSorting
    - QuickSort
    - MergeSort    

#algorithm
    
